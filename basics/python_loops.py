# while, For, Foreach

counter = int(input("Enter the starting value: "))
stop = int(input("Enter the stopping value: "))


while counter <= stop:
    print(f"Counting {counter}")
    counter += 1
